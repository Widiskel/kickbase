global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - "/etc/prometheus/prometheus_rules.yml"

scrape_configs:
  - job_name: 'kickbase'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['app:8080']
